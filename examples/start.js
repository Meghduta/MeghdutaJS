'use strict'

// const meghduta = require('meghduta') // use this instead if this lib is dependancy in your project

const meghduta = require('../app')

meghduta.start() // meghduta.start(httpPort = 6600, websocketPort = 6610)